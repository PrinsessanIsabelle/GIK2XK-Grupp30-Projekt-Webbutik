// Enligt Mikaela är inte denna route prio för oss just nu pga tidsbrist. Viktigast med products.

const router = require('express').Router();

module.exports = router;

// Vi skulle kunna göra en minimal route file with only 2-3 endpoints eventuellt enligt Copilot. 